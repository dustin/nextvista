#!/bin/sh

chmod -R u+rwX,g+rX,o+rX nextvista templates

rsync -vaSe ssh --delete nextvista/ \
	basket.west.spy.net:/data/web/django/nextvista/
rsync -vaSe ssh --delete \
	templates/ basket.west.spy.net:/data/web/django/templates/nextvista/
